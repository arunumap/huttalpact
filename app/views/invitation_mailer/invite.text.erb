You are invited to PactBadger

<%= @inviter.full_name %> invited you to join <%= @organization.name %> on PactBadger.

Accept your invitation:
<%= @accept_url %>

This invitation expires on <%= l(@invitation.expires_at.to_date, format: :long) if @invitation.expires_at %>.
