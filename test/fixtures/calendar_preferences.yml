google_preference:
  calendar_connection: google_connection
  user: one
  organization: one
  remote_calendar_id: "primary"
  remote_calendar_name: "My Calendar"
  sync_enabled: true
  enabled_categories: <%= [].to_json %>
